%h2= t("users.new_title")
= semantic_form_for @user, :url => user_registration_path do |f|
  = f.inputs :name => t("formtastic.group_names.user.attributes") do
    = f.input :email
    = f.input :password
    = f.input :password_confirmation
  
  = f.buttons